# Copy to .env.local for local dev, or set in Vercel → Settings → Environment
# Variables per client deployment. Both are optional — unset falls back to
# the shared GRIMHART demo endpoint. See google-apps-script/README.md.
NEXT_PUBLIC_CALENDAR_ENDPOINT_URL=
NEXT_PUBLIC_CALENDAR_DEMO_TOKEN=
